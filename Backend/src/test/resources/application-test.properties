spring.application.name=lastro

# Jwt Configuration (test)
jwt.public.key=classpath:test-app.pub
jwt.private.key=classpath:test-app.key
jwt.token.expires.in=3600

# Setup inicial
bootstrap.admin.email=bootstrap-test@lastro.test
bootstrap.admin.password=senha-bootstrap-123
bootstrap.admin.presentation-name=Diretoria de Teste

app.storage.root=${java.io.tmpdir}/lastro-test-uploads
app.storage.public-base-url=http://localhost
app.storage.cleanup-enabled=false
app.mail.enabled=false
app.email.queue.enabled=false
app.email.queue.worker-enabled=false
